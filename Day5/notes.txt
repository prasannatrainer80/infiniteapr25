Generics Provides an opportunity to pass or define data types at runtime. 
If you want to make any method as write once and apply for all types, then we need the help of generics. 

The major part of generics used to provide type-safety ensures
only specific data records to be stored in collection. 

Generics can be represented by using <T> annotation. 

Means, in place of <T> we can pass any value type or
reference type or custom type. 

If you have different data types to be passed then you can 
define as 

<T,V> 

Advantages : 
____________

	-> Write Once and apply for all types
	-> Provides the security to the data
	-> No Typecasting Required
	-> Reduces the size of the program. 

Internally how generics work is, assume you have a method as 

void swap(T a, T b) { .... }

now if you pass 

swap(12,13) -> Generics have feature known as code bloat as JVM
generics relevant method in memory and gets executed. Means 

swap(int a, int b) method can be called 

swap("Sandhan","Sourav") -> Generics code bloat generate method in memory as
swap(String a, String b) 

Map interface : This interface is outside the collections framework and used to store values w.r.t. Key/Value pairs. 

Here the key must be unique, and value can be duplicated. 

HashTable and HashMap are falling under Map interface. 

Map<K,V> obj = new HashTable<K,V>();

put() -> This method allows you to add the data into that. 

Exception Handling : 
_____________________

It is a mechanism of handling the runtime errors during the execution of the program. There are 2 types of errors known as 

	(I) Compile Time 

	(ii) Runtime 

Runtime errors occurs, during runtime if we pass invalid values, the type of errors we are getting known as runtime errors. 

By using Exception Handling, we can overcome the runtime errors. 

To handle exceptions, we have 3 blocks known as 

(I) try block : This block contains statement(s) in which there is possibility of getting runtime errors.  The statement(s) which are placed in this block are treated as heavy weighted statement(s) as JVM allocates excess memory to them. 

(ii) catch block : Once exception raised in try block, then the control will be forwarded to catch block, this block contains alternate statement(s) for exceptions raised in try block, one try block can contains multiple catch blocks. 

(iii) finally block : This block contains statement(s) that needs to be executed mandatorily irrespective of try and catch blocks, this block is not mandatory, but all the statement(s) which are placed in this block are executed as mandatory statements.

Exception is the top most class, from which all kinds of exceptions are derived. 

syntax :
_________

try {
...
...
...
} catch (Exception e) {
...
...
...
} finally {
...
...
...
}

printStackTrace() : An exception contains 

	-> Exception Class
	-> Error Message
	-> Line Number
	-> Char Pos

All you need to print at once, we need the help of printStackTrace().









